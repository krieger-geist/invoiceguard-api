package com.invoiceguard.organization.entity;

/** Lifecycle status of a user's membership within a specific organisation. */
public enum OrganizationMemberStatus {
    ACTIVE,
    INVITED,
    DISABLED
}
